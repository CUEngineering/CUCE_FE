{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20",
    "npm": ">=10.2.3"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "use-node": "nvm use",
    "setup": "nvm use && npm install",
    "clean-start": "rm -rf node_modules .nuxt && npm install && npm run dev"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.11.0",
    "@tanstack/vue-table": "^8.21.2",
    "@vueuse/core": "^13.0.0",
    "axios": "^1.9.0",
    "date-fns": "^4.1.0",
    "nuxt": "^3.8.0",
    "pinia-plugin-persistedstate": "^4.3.0",
    "vue": "^3.3.4",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "sass": "^1.85.1"
  }
}
